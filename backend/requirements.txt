# --- Machine Learning ---
pandas~=2.2
numpy~=1.26
scikit-learn~=1.5
xgboost~=2.1
joblib~=1.4

# --- Backend / API ---
fastapi~=0.115
uvicorn[standard]~=0.32
python-multipart~=0.0.12    # required by FastAPI for file uploads
pydantic~=2.9
pydantic-settings~=2.6      # loads settings from .env into typed classes

# --- Database ---
sqlalchemy~=2.0
psycopg2-binary~=2.9        # PostgreSQL driver

# --- Config / Utilities ---
python-dotenv~=1.0

# --- Testing ---
pytest~=8.3
httpx~=0.27                 # needed by FastAPI's TestClient
